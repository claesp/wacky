{{define "content" -}}
{{template "breadcrumbs" dict "Trail" .Breadcrumbs "Path" .Path}}

<article class="page">
  <h1>{{.Heading}}</h1>
  <h2>History</h2>

  {{if .Commits}}
  <div class="table-wrap">
    <table>
      <thead>
        <tr><th>When</th><th>Author</th><th>Subject</th></tr>
      </thead>
      <tbody>
        {{- range .Commits}}
        <tr>
          <td>{{formatTime .When}}</td>
          <td>{{.Author}}</td>
          <td>{{template "commit-subject" dict "URL" $.CommitURL "Commit" .}}</td>
        </tr>
        {{- end}}
      </tbody>
      {{- if .Truncated}}
      <tfoot>
        <tr><td class="muted" colspan="3">Only showing the latest {{.Limit}} commits</td></tr>
      </tfoot>
      {{- end}}
    </table>
  </div>
  {{else}}
  <p>This file has no commits yet.</p>
  {{end}}
</article>

{{template "page-meta" dict "Path" .RepoPath "Commit" .LastCommit "HasHistory" .HasHistory "CommitURL" .CommitURL}}
{{- end}}
